<Page x:Class="WPF.ExportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Export Drive Data">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Select data to export:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Grid.Row="0"/>
        <CheckBox x:Name="chkParameterData" Content="Setup List" Margin="0,10,0,0" Grid.Row="1"/>
        <CheckBox x:Name="chkTelegramData" Content="Friction Table" Margin="0,10,0,0" Grid.Row="2"/>
        <CheckBox x:Name="chkAxisData" Content="Technology Controller" Margin="0,10,0,10" Grid.Row="3"/>
        <TreeView Grid.Row="4" Margin="0,10,0,10"/>
        <Button Content="Next" Width="100" Height="30" HorizontalAlignment="Right" Margin="0,10,0,0"
                VerticalAlignment="Bottom" Grid.Row="5" Click="BtnNext_Click"/>
    </Grid>
</Page>

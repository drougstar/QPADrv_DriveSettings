<Window x:Class="WPF.ExportPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Drive Data" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Select data to export:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

        <CheckBox x:Name="chkParameterData" Content="Setup List" Margin="0,19,0,115" Grid.RowSpan="4" Checked="chkParameterData_Checked" />
        <CheckBox x:Name="chkTelegramData" Content="Friction Table" Margin="0,40,0,0" Checked="chkTelegramData_Checked" />
        <CheckBox x:Name="chkAxisData" Content="Technology Controller" Margin="0,60,0,10" Checked="chkAxisData_Checked" />

        <Button Content="Next" Width="100" Height="30" HorizontalAlignment="Right" Margin="0,110,0,0"
                VerticalAlignment="Top" Click="BtnNext_Click" />
        <TreeView Grid.Row="3"/>
    </Grid>
</Window>